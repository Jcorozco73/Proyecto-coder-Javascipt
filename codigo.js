let edad = false
let = validarCliente = function (time) {
    edad = prompt( "Cual es tu edad?")
    if (edad > 18) {
      if (time >= 2 && time < 7 && free == false) {
        alert(" Podes pasar, porque sos la primera persona despues de las 2 am")
        free = true
      } else {
        alert(` son las ${time}:00 y podes pasar, pero tenes que pagar`)
      } 
  
    } else {
      alert("Sos menor de edad por ende no podes paasr")
    }
    }
